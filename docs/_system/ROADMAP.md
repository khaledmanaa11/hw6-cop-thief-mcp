# ROADMAP — HW6 Dual AI Agent (Cop & Thief) via MCP

Single source of truth for **where we are**. Every new session reads this first.
Source: assignment §13, Table 4 (recommended engineering priority order).

## Status legend

| Mark | Meaning |
|------|---------|
| ⬜ | not started |
| 🗣️ | being discussed (Planner session in progress) |
| 📄 | DECISION written |
| 📦 | triplet (PRD/PLAN/TODO) built |
| 🔨 | implementing (Developer working through TODO) |
| ✅ | done (all TODO boxes pass + PRD acceptance met) |

## The 8 steps

| # | Slug / folder | Step | What it adds to the project | Status |
|---|---------------|------|------------------------------|--------|
| 1 | `step1_game_logic`     | Game logic & rules        | Pure engine: grid, movement, barriers, capture, scoring, sub-game/game loop, `config.yaml`. No MCP, no LLM. | ✅ |
| 2 | `step2_mcp_infra`      | Basic MCP infrastructure  | Two separate FastMCP servers (Cop, Thief) exposing tools (validate-location, send-message). Plumbing only. | ✅ |
| 3 | `step3_local_run`      | Full local run            | Wire engine + both agents + both servers on localhost (separate ports); a full 6-sub-game series runs end-to-end locally. | ✅ |
| 4 | `step4_decision_engine`| Decision mechanism        | The strategy "brain": heuristic / distance-based / Q-Table so agents pursue & evade. | ✅ |
| 5 | `step5_nl_integration` | Natural-language integration | Replace structured messages with free-text LLM messages — agents talk, infer location, deceive. (Graded core.) | ✅ |
| 6 | `step6_gui`            | Graphical user interface  | Visual board: real-time movement, barriers, logs. | ⬜ |
| 7 | `step7_cloud_deploy`   | Cloud distributed deployment | Push MCP servers to cloud with tokens/secure tunnels/cyber hygiene; publish 2 public URLs. | ⬜ |
| 8 | `step8_gmail_api`      | Gmail API reporting       | Auto-send the JSON-only report email to the lecturer after 6 sub-games. | ⬜ |

## Cross-cutting deliverables (not a "step", tracked separately)

- `README.md` with the **Dec-POMDP** formal model, architecture-challenge analysis,
  and visual/CLI proof of correct communication.
- `config.yaml` (single source of game parameters — no hard-coding).
- Sanity-check escalation across grids: 2×2 → 3×3 → 4×4 → 5×5.
- Optional **inter-group bonus** competition (worth up to 10 pts, separate JSON report).

## Fixed parameters (from the assignment — do not drift)

| Key | Default | Meaning |
|-----|---------|---------|
| `grid_size` | `[5, 5]` | board dimensions (must be resizable) |
| `max_moves` | `25` | max moves per sub-game |
| `num_games` | `6` | sub-games per series |
| `max_barriers` | `5` | barriers the Cop may place per sub-game |
| `scoring.cop_win` | `20` | Cop score when Cop captures |
| `scoring.thief_win` | `10` | Thief score when Thief survives |
| `scoring.cop_loss` | `5` | Cop score when Thief survives |
| `scoring.thief_loss` | `5` | Thief score when captured |

Report email target: `rmisegal+uoh26b@gmail.com` (JSON body only).

## Progress log

<!-- Append one line per session: date — step — what changed. Newest at bottom. -->
- 2026-06-25 — system — scaffolded `docs/_system/` (WORKFLOW, ROADMAP, 4 templates). No step content yet.
- 2026-06-25 — step1 — Planner session: read PDF §4, locked all rules, wrote `DECISION_step1_game_logic.md`. Step 1 → 📄. Next: Builder session (triplet).
- 2026-06-25 — step1 — Builder session: produced `PRD_step1_game_logic.md`, `PLAN_step1_game_logic.md`, and `TODO_step1_game_logic.md`. Step 1 → 📦.
- 2026-06-25 — step1 — Triplet review: patched TODO+PLAN — added boxes for `apply_move`, result dataclasses, `initial_state`/start positions, deps+packaging (pyyaml/pyproject), empty-legal-moves skip, barrier mechanics, CLI `__main__`, and illegal-move/barrier tests. Step 1 stays 📦, ready for Developer.
- 2026-06-25 — step1 — Developer session: implemented all A–G boxes, 26 tests pass, `python -m src.game` runs 6 sub-games. Step 1 → ✅. (TODO checkboxes were left unticked; reconciled after verifying tests.)
- 2026-06-25 — step1 — Fix: `play_series` now swaps Cop/Thief roles each sub-game between two groups (group_a/group_b totals, §4.4 90/30 bound). Updated engine, CLI, tests (27 pass), and DECISION/PRD/PLAN/TODO to match.
- 2026-06-25 — step2 — Planner session: locked state model (referee owns state, stateless servers), transport (streamable-http on config ports, Cop 8001/Thief 8002), lib (standalone `fastmcp` 3.x), tool subset (ping/validate_location/validate_move/send_message both; place_barrier-validate Cop-only), and 3 creative seams (rich `{ok,reason}` returns, free-text-only message envelope, partial-observation seam). Wrote `DECISION_step2_mcp_infra.md`. Step 2 → 📄. Next: Builder session (triplet).
- 2026-06-25 — step2 — Builder session: verified live `fastmcp` v3.2.4 API via context7 (`FastMCP`, `@mcp.tool`, `mcp.run(transport="http", host, port)`, in-memory `Client(server)`, `result.data`). Produced `PRD_/PLAN_/TODO_step2_mcp_infra.md`. TODO is fat/atomic (A1–F3, ~30 boxes) with full copy-paste code for a weak Developer: deps+`servers:` config block, `tools.py` pure adapters, `cop_server.py`/`thief_server.py` builders+`__main__`, and per-tool in-memory tests incl. asymmetry + 3×3 resizability. Step 2 → 📦. Next: Developer session (implement TODO box-by-box).
- 2026-06-25 — step2 — Developer session: implemented all A–F boxes (Phase A: deps/config, Phase B: tools.py adapters, Phase C: cop_server, Phase D: thief_server, Phase E: 15 in-memory Client tests, Phase F: full verification). 39 tests pass (27 Step-1 + 12 Step-2). No hard-coded ports or grid params in mcp_servers/. Asymmetry holds (place_barrier Cop-only). Both servers build ok. Step 2 → ✅.
- 2026-06-25 — step2 — Triplet reconciliation: Developer had left TODO boxes unticked. Re-verified every box against real evidence (re-ran `pytest -q`=39 pass, F2 grep clean, F3 both-build ok, confirmed all 12 named Step-2 tests + 6 tools.py adapters + asymmetry). All PRD §7 acceptance criteria hold. Ticked all 44 TODO boxes + Definition of Done; set PRD status → done. No code changes needed — work was genuinely complete.
- 2026-06-25 — step3 — Planner session: locked the wiring design. Referee-authoritative orchestrator (`src/orchestrator/`) connects as HTTP client to MANUALLY-started servers; servers stay validate-only (referee applies via engine); orchestrator is the message bus; minimal `GreedyMover` (Chebyshev pursue/evade, deterministic tie-break, no barriers) drives agents — full heuristic+Q-Table stays Step 4. Key seam: `ServerGateway` protocol with `HttpGateway` (prod) + `InMemoryGateway` (port-free tests). No new config keys. Wrote `DECISION_step3_local_run.md`. Step 3 → 📄. Next: Builder session (triplet). Builder must verify live `fastmcp` v3 HTTP **client** URL/path via context7.
- 2026-06-25 — step3 — Planner (creative pass): Director approved 4 instrumentation extras, folded into the DECISION as `recorders.py` seams (produced now, consumed by Steps 5/6/8): (1) JSONL replay log `runs/<ts>.jsonl` — architectural spine; (2) network telemetry (boot ping + per-call latency, timed in the gateway); (3) Dec-POMDP per-agent observation log with stable `sees_opponent`/`opponent_pos` schema for Step 5; (4) ASCII board + readable transcript (README CLI proof). Added optional operational config key `output.run_dir` (default `runs`, trailing optional like `servers`). Acceptance criteria 7–10 added.
- 2026-06-26 — step3 — Developer session: implemented all A–E boxes. New package `src/orchestrator/` (gateway.py, referee.py, recorders.py, __main__.py, __init__.py). Added GreedyMover to src/game/movers.py. 63 tests pass (39 old + 24 new orchestrator). No hard-coded ports/grid params in src/orchestrator/. ServerGateway protocol + InMemoryGateway (port-free tests) + HttpGateway (live run). Message bus, JSONL replay log, telemetry, Dec-POMDP observation log, ASCII render all wired. Step 3 → ✅. Next: Step 4 (decision engine — heuristic / Q-Table).
- 2026-06-26 — step4 — Planner session: empirically playtested the real engine before deciding. Findings: (1) default 5×5 is a forced Cop win (greedy Cop beats every Thief 100%, ~7 plies, 0 barriers) ⇒ bonus competition ties 75–75 for all; (2) skill only differentiates on bigger boards, where minimax Cop wins 100% vs greedy's ~20% (9×9/25); (3) barriers are strategically inert for the Cop under 8-dir (tested own-cell/adjacent/free — optimal Cop places 0). Verified §4.2 **mandates 8-dir incl. diagonals** (4-dir pivot rejected) and §4.3 fixes the own-cell barrier rule; §2/§3 say strategy is NOT graded (orchestration is); §8 marks Q-Table optional. DECISION: config-selectable Mover family — `MinimaxMover` (primary, alpha-beta + tunable eval, considers PLACE_BARRIER) + optional `QTableMover` (offline-trained, committed table) + keep `GreedyMover`; new `src/strategy/` package + `strategy:` config block; movement/barriers unchanged; follow official 5×5. Wrote `DECISION_step4_decision_engine.md`. Step 4 → 📄. Next: Builder session (triplet). Builder gotcha: minimax MUST clone state+board per node (apply_move mutates the shared Board on PLACE_BARRIER).
- 2026-06-26 — step4 — Developer session: implemented all A–D boxes. New package `src/strategy/` (evaluate.py, minimax.py, qtable.py, train.py, factory.py, __init__.py). Added strategy: block to config.yaml and optional Config.strategy field. 91 tests pass (63 old + 28 new strategy). MinimaxMover alpha-beta (Cop=MAX/Thief=MIN, PLACE_BARRIER considered, clone-safe). QTableMover with board-size-agnostic key + greedy fallback. models/qtable.json trained (20k episodes, seed 7) and committed. Grep gate clean. Step 4 → ✅. Next: Step 5 (NL integration).
- 2026-06-26 — step5 — Planner session (the GRADED CORE). Locked the NL design via 4 Director calls: (1) **Hybrid brain** — LLM (Claude) proposes move+message+belief, Step-4 minimax vetoes only illegal/blunder moves (LLM is part of the choice); (2) **model `claude-opus-4-8`** in config, `FakeLLM` in tests (model config-selectable); (3) **partial observation** — implement ALL THREE modes `blind|noisy|full` config-selectable, default `noisy`, keep the choice open for testing (`blind` = strongest Dec-POMDP claim); (4) **reproducibility by recording** real LLM I/O to JSONL, determinism lives in FakeLLM tests. New client-tier pkg `src/agents/` (llm_client.py with injectable `LLMClient`=`AnthropicLLM`/`FakeLLM`, agent.py `LLMAgent`+`MoverAgent` adapter, prompts.py, factory.py). `recorders.observe()` upgraded to partial obs; referee drives `Agent`s + real NL message bus into per-side inboxes. 3 creative seams folded in: belief-accuracy metric (Chebyshev error inferred-vs-true), LLM telemetry (latency/tokens/cost), prompt caching of the static rules prompt. LLM in client only; servers untouched; API key via env. Wrote `DECISION_step5_nl_integration.md`. Step 5 → 📄. Next: Builder session (triplet) — MUST verify live `anthropic` SDK structured-output call (output_config.format, no temperature/budget_tokens on 4.8, cache_control on system) via context7/claude-api before writing the TODO.
- 2026-06-26 — step5 — Planner (persona + simulation pass): ran an offline deception simulation (`docs/step5_nl_integration/decep_sim.py`, 4000 games/matchup on real engine, NO LLM). Proved: truth-telling is self-sabotage; a *consistent* liar Thief breaks a believing Cop to **0% capture**; skepticism defends; deception is asymmetric (Thief win-condition vs Cop flush-tool); exploit-naive / play-sound-vs-robust is the winning meta. Locked two personas — **THE DETECTIVE (Cop)** = extraction + contradiction-tracking + herding; **THE GHOST (Thief)** = consistent disinformation + interrogate-the-Cop + herding-detection. Both ask each other questions by design. Added §11 to the DECISION: simulation table, personas, draft system prompts, structured output schema `{opponent_guess,confidence,move,message,intent,reasoning}` (reasoning PRIVATE, never sent), per-turn user template, and 3 doctrine test hooks. Refined the hybrid veto to be **confidence-weighted** (high-confidence ⇒ trust LLM move; low ⇒ minimax dominates). Step 5 stays 📄.
- 2026-06-26 — step5 — Planner (barrier-deception pass, Director request): added two tactics to the personas. COP = **barrier/region bluff** (place real barriers to pinch the escape region + tell the Thief "the far half is sealed, you're trapped on my side" — unfalsifiable in blind mode; sim Experiment D shows ~17% faster capture on 5×5 vs a believer, skeptic immune). THIEF = **invent obstacles** ("a barrier boxed me in") to corroborate the decoy. Key insight folded into DECISION §11.2 + README: **barrier asymmetry** — barriers are Cop-only/Cop-known, so the Cop's "sealed half" bluff is unverifiable (strong) while the Thief's phantom-barrier claim is verifiable by the Cop (a tell) — this revives the Step-4-inert barrier as a *bluff prop*. Added `intent:"trap"` to the schema and a **veto exemption** so a trap-barrier isn't killed by minimax (still legal + tempo-budgeted; the verbal bluff is a free message). Updated both system-prompt drafts. Sim Experiment D archived in `decep_sim.py`. Step 5 stays 📄.
- 2026-06-27 — step5 — Planner (persona-voice pass, Director request): rewrote DECISION §11.4 system prompts to crank the *voice* to **🕵️ THE DETECTIVE = "the dirty cop"** and **👻 THE GHOST = "the best liar in the world"** — taunting, theatrical, psychologically manipulative. Tactics unchanged (region bluff, phantom barriers, herding, skepticism, contradiction-tracking all preserved). Locked a **two-channel discipline**: real read/plan lives ONLY in private `reasoning`, the loud trash-talk lives in `message` — which makes the savage tone *grading-safe* (the message channel is meant to be adversarial free text, so it strengthens the §2/§3 NL/deception grade) because the visible message carries no real info by construction. Added **level-2 theory-of-mind** framing (model the opponent's belief about you, then corrupt it) and concrete taunt exemplars ("I think I'm right behind you", "your sealed half is adorable — I strolled past it"). Tone is a one-line dial in `prompts.py`. Step 5 stays 📄. Next: Builder session (triplet) — still MUST verify live `anthropic` structured-output SDK call before writing TODO.
- 2026-06-27 — step5 — Director call: switched default LLM from `claude-opus-4-8` → **`claude-haiku-4-5-20251001`** (no inter-group competition this run, so cheaper/faster Haiku is plenty; model stays config-selectable — one line to swap back to Opus for a competitive run). Updated all 6 model refs in DECISION (§2 scope, §4 table w/ rationale, §7 llm_client, §9 Q+Builder note, §10 config snippet). Also corrected the SDK caveat: **`temperature` is supported on Haiku 4.5** (the "no sampling params / they 400" rule was Opus-4.8-only) — added `temperature: 0.7` to the config snippet (helps generate varied/creative lies; Builder still re-confirms the live SDK for the configured model). Step 5 stays 📄.
- 2026-06-27 — step5 — Builder session (delegated to Codex CLI, gpt-5.5). First wrote `SDK_REFERENCE_anthropic.md` — the VERIFIED live `anthropic` Haiku-4.5 structured-output call (`output_config={"format":{"type":"json_schema","schema":…}}`, `temperature` OK on Haiku, `cache_control` ephemeral system block, `usage.cache_read_input_tokens` telemetry) via the claude-api skill, so Codex couldn't guess a wrong SDK shape. Codex then produced the triplet: `PRD_` (13 acceptance criteria), `PLAN_` (architecture + file map + risks), `TODO_` (61 atomic boxes, Phases A–J, copy-paste code for `llm_client.py`+`agent.py`, acceptance-coverage matrix). Claude reviewed: verified EVERY load-bearing signature against the real code (GameState in state.py, Move/legal_moves/apply_move, MinimaxMover(depth,weights,max_moves), evaluate(state,config), build_mover, config.strategy["minimax"], run_sub_game/run_series cop_mover/thief_mover/group_a/group_b/a_is_cop, observe(state,side,last_msg) backward-compat, Config trailing-optional order) — all correct. Triplet approved. Step 5 → 📦. Next: Developer session (Codex) implements the TODO box-by-box + commit.
- 2026-06-27 — step5 — Developer session: implemented Step 5 NL integration (`src/agents/` LLM seam/prompts/hybrid agent/factory, config defaults, partial observations, LLM telemetry, referee inbox bus/replay action records, orchestrator live wiring, doctrine/static tests). Final automated suite: 130 passed. Deviation: J4 live Anthropic run not executed because this sandbox has no real API key/server terminals/external network access.
